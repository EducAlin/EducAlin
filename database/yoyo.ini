# Yoyo migrations configuration file
[DEFAULT]
# Path to migration scripts
sources = migrations

# Database connection string
# For SQLite: sqlite:///path/to/database.db
# For production, use environment variable
database = sqlite:///educalin.db

# Batch mode off for better control
batch_mode = off

# Verbosity level
verbosity = 2
